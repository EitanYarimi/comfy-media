# --- Local mode (Mac + Google Drive desktop sync) ---
STORAGE_MODE=local
MEDIA_ROOT="/Users/eitanmac14/Library/CloudStorage/GoogleDrive-eitanyarimi@gmail.com/My Drive"

# --- Cloud mode (Render/Railway — reads Drive via API, no local sync) ---
# STORAGE_MODE=drive
# DRIVE_ROOT_FOLDER_ID=your_shared_folder_id
# GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

# Optional overrides (paths are relative to MEDIA_ROOT or Drive root folder):
# VIDEO_DIR=ComfyUI/output/video
# PHOTO_DIRS=ComfyUI/output,stable-diffusion-webui/outputs

# Thumbnail/stream cache on local disk:
# MEDIA_CACHE_DIR="$HOME/Library/Caches/comfy-media-server"

# Server port (cloud hosts set PORT automatically):
# PORT=8080
