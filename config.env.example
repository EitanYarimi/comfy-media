# Copy to config.env and adjust paths for your Mac.
# Google Drive desktop sync path (update the account folder name if needed):
MEDIA_ROOT="/Users/eitanmac14/Library/CloudStorage/GoogleDrive-eitanyarimi@gmail.com/My Drive"

# Optional overrides (paths are relative to MEDIA_ROOT):
# VIDEO_DIR=ComfyUI/output/video
# PHOTO_DIRS=ComfyUI/output,stable-diffusion-webui/outputs

# Thumbnail/stream cache stays on local disk (fast, no Drive sync churn):
# MEDIA_CACHE_DIR="$HOME/Library/Caches/comfy-media-server"

# Server port (default 8080):
# PORT=8080
